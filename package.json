{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Made a root ",
  "main": "index.js",
  "scripts": {
    "client-build": "cd client && npm install && npm run build",
    "server-build": "npx tsx ./api/chat/index.ts",
    "build": "npm run client-build && npm run server-build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^22.15.12"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  }
}
